# Silicon Sample Benchmark — Tier 3 RAG Pipeline
# Team: Farah Adeeba, Jing Ma, Max Pellert, Marcia Ferreira
# Install: pip install -r requirements.txt

# LLM APIs
openai>=1.0.0          # GPT-4o calls + text-embedding-3-small
anthropic>=0.50.0      # Claude (optional, for cross-model comparison)

# Vector database
chromadb>=1.0.0        # RAG vector store (persistent, local)

# PDF text extraction (for add_paper.py)
pymupdf>=1.23.0        # pip install pymupdf